# TFL 경기 관전 UI 1차 인터랙티브 프로토타입

이 폴더는 대표님 / 봉이사 검토를 위한 브라우저 확인용 인터랙티브 프로토타입이다.

```txt
목업 파일: docs/design/mockups/match-viewer-v1/index.html
스타일 파일: docs/design/mockups/match-viewer-v1/styles.css
샘플 데이터: docs/design/mockups/match-viewer-v1/sample-data.js
인터랙션 스크립트: docs/design/mockups/match-viewer-v1/prototype.js
```

## 목적

```txt
정적 목업만으로 판단하기 어려운 탭 전환, Play Focus View 움직임, 이벤트 연동 감각을 확인한다.
PC 가로 화면과 모바일 세로 화면에서 탭 기반 인게임 UI 사용감을 확인한다.
개발팀장 UI 구현 전 대표님 / 봉이사 컨펌 기준으로 사용한다.
```

## 포함 화면

```txt
1. LIVE 탭 인터랙션
2. 선수 / 교체 탭 인터랙션
3. 작전 / 전술 탭 인터랙션
4. 기록 / 분석 탭 인터랙션
5. PC 가로 화면 / 모바일 세로 화면 반응형
```

## 포함 요소

```txt
경기 시간 / 스코어 / 팀명
2D 경기장
공격 루트 표시
주요 이벤트 표시
간단 경기 기록
텍스트 중계
상대 채팅 정적 슬롯
실시간 개입 버튼
현재 포메이션
출전 선수 체력 / 컨디션 / 상태
경고 / 부상 위험 / 집중도 표시
교체 선수 카드 목록
교체 가능 횟수
교체 지정 UI
현재 전술
공격 성향 / 수비 라인 / 압박 강도 / 패스 성향
공격 방향 지시
감독 스킬 슬롯
전술 게이지
스킬 쿨타임
팀 상세 기록
선수별 기록
공격 방향 비율
이벤트 타임라인
Momentum 흐름
샘플형 히트맵 / 방향성 시각화
Play Focus View 샘플 이벤트 7종
텍스트 중계와 이벤트 타임라인의 현재 이벤트 연결
```

## v0.3 보정 내용

```txt
드리블 이동은 경기장 기준 2~4% 안쪽의 짧은 전진으로 제한했다.
드리블 중 공은 선수 발밑 주변에서만 0.2초 단위의 짧은 터치처럼 움직인다.
패스 후보는 카드가 아니라 바둑알과 작은 라벨로만 표시한다.
선수 카드는 임의 좌표 오프셋이 아니라 자기 바둑알 중심 기준 top / bottom / left / right 앵커로만 배치한다.
공 소유 선수 카드는 항상 자기 바둑알 위쪽 중앙 정렬을 기준으로 한다.
수비수 카드는 태클 / 슛 방어 등 핵심 순간에만 표시하고, 표시 시에도 자기 바둑알에 앵커링한다.
불명확한 노란 초점 영역 / 강조 박스는 제거 상태를 유지한다.
크로스 / 롱패스는 실제 x/y 경로를 꺾지 않고, 9단계 스케일 변화와 그림자만으로 체공감을 보정했다.
모바일에서도 카드가 자기 바둑알에서 멀리 밀려나지 않도록 카드 폭과 앵커 거리를 줄였다.
```

## 제외 범위

```txt
실제 제품 코드
경기엔진 연동
API / DB 연동
로그인 / 결제 / 리그 / 시즌 / 어드민 / 커뮤니티 구현
실제 실시간 채팅 구현
정교한 좌표 기반 히트맵
실제 선수 데이터
실제 선수명
실제 선수 능력치
22명 전체 정밀 실시간 이동
정밀 좌표 기반 히트맵
```

상대 채팅은 대표님 상세 검토 요청을 반영한 정적 UI 위치 검토 슬롯이다.
실시간 채팅, 커뮤니티, 서버 연동 구현 범위를 확정하는 의미가 아니다.

## 인게임 연출 기준

```txt
모바일 세로 화면에서도 그라운드는 세로형 경기장으로 만들지 않는다.
모바일은 세로 UI 안에 가로형 경기장을 카드처럼 배치한다.
선수 표현은 22명 전체 상시 표시 방식도 아니고 공 가진 선수 1명만 표시하는 방식도 아니다.
현재 플레이 이벤트에 관여한 선수만 표시하는 Play Focus View를 기준으로 한다.
```

Play Focus View 이벤트별 표시 기준:

```txt
드리블: 공 소유 선수 + 압박 수비수
태클: 공 소유 선수 + 태클 시도 선수
패스: 패스한 선수 + 패스 받을 선수
크로스: 크로스 올리는 선수 + 받을 선수 + 마크 수비수 + 골키퍼
롱패스: 패스한 선수 + 수신 후보 + 경합 수비수 + 골키퍼
슛: 슈팅 선수 + 골키퍼 + 필요 시 가까운 수비수
골: 득점 선수 + 어시스트 선수 + 골키퍼
```

선수 강조 기준:

```txt
핵심 선수는 카드 UI로 크게 강조한다.
보조 선수는 바둑알, 작은 라벨, 등번호 중심으로 표시한다.
나머지 선수는 표시하지 않거나 흐릿한 배경 점 정도로만 처리한다.
목표는 FM식 22명 정밀 실시간 이동 구현이 아니라 TFL만의 이벤트 중심 플레이 포커스 연출로 보는맛을 만드는 것이다.
```

## 데이터 구조 기준

```txt
sampleMatchData
samplePlayers
sampleEvents
sampleStats
sampleTactics
```

위 샘플 데이터는 `sample-data.js`에 분리되어 있다.
추후 API 연동 시에는 화면 렌더링 구조를 유지하고 데이터 공급원만 교체하는 방향을 전제로 한다.

```txt
DB 호출 없음
API 호출 없음
서버 통신 없음
실제 경기엔진 연동 없음
실제 선수 데이터 없음
```

## PC 확인 방법

브라우저에서 다음 파일을 열고 가로 화면 기준으로 확인한다.

```txt
C:\workSpace\TFL-development\docs\design\mockups\match-viewer-v1\index.html
```

권장 확인 폭:

```txt
1280px 이상
```

## 모바일 확인 방법

같은 파일을 브라우저에서 연 뒤 개발자도구 또는 반응형 보기에서 세로 화면으로 확인한다.

권장 확인 폭:

```txt
390px x 844px
430px x 932px
```

이 프로토타입은 HTML / CSS / JavaScript만 사용한다.
개발팀장이 그대로 복사해서 제품 코드로 사용하는 것이 아니라, 움직임과 탭 사용감 확인용 산출물이다.
